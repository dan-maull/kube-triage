[project]
name = "kube-triage"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.34.2",
    "typer>=0.27.1",
]

[project.scripts]
kube-triage = "kube_triage.main:app"

[tool.uv]
package = true
